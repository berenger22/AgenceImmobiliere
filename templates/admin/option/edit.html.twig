{% extends 'base.html.twig' %}

{% block title %}Agence Immobiliere{% endblock %}

{% block monTitre %}
{% if isModification %}
Editer l'option
{% else %}
Ajouter une option
{% endif %}
{% endblock %}
{% block body %}
    {{form_start(form)}}
    {{form_rest(form)}}
    <button class="btn btn-primary">Enregistrer</button>
    <a href="{{path('adminOptionIndex')}}" class="btn btn-secondary ml-3">Retour</a>
    {{form_end(form)}}
{% endblock %}
